import React from "react";
import UserGraph from "../components/UserGraph"; // Adjust path if needed

const Users = () => {
    return (
        <div>
            <UserGraph />
        </div>
    );
};

export default Users;
